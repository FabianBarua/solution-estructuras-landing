---
interface Props {
	className?: string
	id?: string
}

const { className, id } = Astro.props
---

<section id={id} class={`w-full px-2 lg:px-0 max-w-4xl mx-auto mt-2 ${className}`}>
	<slot />
</section>
